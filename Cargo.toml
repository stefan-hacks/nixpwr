[package]
name = "nixpwr"
version = "0.1.0"
edition = "2021"
description = "Linux laptop power diagnostics and policy CLI"
license = "MIT"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.36"
